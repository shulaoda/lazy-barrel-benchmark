{
  "name": "lazy-barrel-benchmark",
  "version": "1.0.0",
  "scripts": {
    "build:rolldown": "rolldown -c rolldown.config.mjs",
    "build:rspack": "rspack --config rspack.config.mjs"
  },
  "devDependencies": {
    "@rspack/cli": "^1.7.2",
    "@rspack/core": "^1.7.2",
    "rolldown": "1.0.0-beta.60"
  },
  "dependencies": {
    "antd": "^6.2.0"
  }
}
